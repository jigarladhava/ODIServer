{
  "name": "@odiserver/drivers",
  "version": "0.1.0",
  "description": "ODIServer southbound drivers: Node-RED flow generation and bridge nodes.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@odiserver/core": "*"
  }
}
